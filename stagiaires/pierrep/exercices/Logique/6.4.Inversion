// Ecrire le programme qui inverse un tableau de 10 nombres.

DEBUT PROGRAMME

    VARIABLES LOCALES :
        CONST TAILLE:N <- 10
        VAR tabIn:N[SIZE]
        VAR tabOut:N[SIZE]
        VAR index
    FIN VARIABLES LOCALES

    
    index <- 1
    POUR index ALLANT DE 1 A SIZE
        tabOut[index] <- tabIn[SIZE - index + 1]

    index SUIVANT

FIN PROGRAMME
