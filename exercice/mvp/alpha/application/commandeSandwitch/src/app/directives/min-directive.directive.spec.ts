import { MinDirectiveDirective } from './min-directive.directive';

describe('MinDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MinDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
